package Package0805;

import java.util.Scanner;

public class Example01_for {
	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);
		
		int a = input.nextInt();
		int b = input.nextInt();
		
		int multiple = a * b;
		
		int max = (a>b) ? a : b;
		int min = (a<b) ? a : b;
		
		while (min % 3 != 0) {
			min++;
		}
		
		if (a<b) {
			for (int i = min; i <= max; i+=3) {
				System.out.println(i);
			}
		} else {
			for (int i = max; i >= min; i-=3) {
				System.out.println(i);
			}
		}
		
		// a와 b를 입력을 받아서 a와 b 사이의 정수들 중 3의 배수를 출력하세요
		// 단, b가 더 클 경우
		// 17 1 일 경우
		// 15 12 9 6 3으로 출력됨.
	}
}